import { useState } from "react"

export default function NavbarDrawer() {

    // this component is in charge of opening and closing the drawer
    const [open, setOpen] = useState(false)

    return (
        <div>

        </div>
    )
}